[project]
name = "clinical-doc-quality-scorer"
version = "0.1.0"
description = "Clinical Document Quality Scorer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
"openai",
"aiofiles",
"python-dotenv",
]
